% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/key.R
\name{key_generate}
\alias{key_generate}
\title{Generate a new age identity (key pair)}
\usage{
key_generate(keyfile = NULL)
}
\arguments{
\item{keyfile}{Character string or NULL. If provided, saves the private key to this file path.
If NULL, returns both public and private keys in memory without saving to disk.}
}
\value{
A \code{lockbox_key} object containing:
\itemize{
\item \verb{$public}: The public key (age recipient identifier)
\item \verb{$private}: The private key (only when \code{keyfile = NULL})
\item \verb{$created}: Timestamp of key creation
}
}
\description{
Create a new age encryption key pair. Keys can be saved to a file or kept in memory.
The key pair consists of a public key (for encryption) and a private key (for decryption).
}
\examples{
\dontrun{
# Generate keys in memory
key <- key_generate()
print(key$public)
print(key$private)

# Generate and save to file
key_generate("my_identity.key")
}

}
